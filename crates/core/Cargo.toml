[package]
name = "bastion-core"
version = "0.1.0"
edition = "2024"

[lib]
path = "src/lib.rs"

[dependencies]
chrono = { workspace = true, default-features = false, features = ["clock", "std"] }
secrecy = { workspace = true }
uuid = { workspace = true }